{
  "name": "electron-jsoneditor",
  "version": "1.0.0",
  "description": "A jsoneditor for electron.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "pack": "electron-packager . JsonEditor --platform=win32 --arch=x64 --out=./release --asar  --app-version=0.0.1 --overwrite --ignore=node_modules",
    "iscc": "iscc ./inno_setup_script/install.iss"
  },
  "keywords": [
    "electron"
  ],
  "author": "Daniel Pine",
  "license": "MIT",
  "devDependencies": {
    "electron": "^9.0.4",
    "electron-winstaller": "^4.0.1"
  },
  "dependencies": {
    "electron-packager": "^15.0.0",
    "electron-squirrel-startup": "^1.0.0"
  }
}
